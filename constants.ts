/**
 * Minimal supported versions.
 *
 * IMPORTANT! Update the compatibility section in README.md when changing the versions.
 */
export const MIN_SUPPORTED_VERSION = {
    /**
     * Similar to the version specified in the filters repo where aglint-cli is used.
     *
     * IMPORTANT! Update the engine section in package.json when changing the version.
     */
    CLI_NODE: 20,
    CHROME: 88,
    FIREFOX: 84,
    EDGE: 88,
    OPERA: 80,
    SAFARI: 14,
};
